import { Typography } from "antd";

const Weather = () => {
  return (
    <Typography.Title
      level={2}
      style={{ textAlign: "center", marginTop: "20px" }}
    >
      Погода
    </Typography.Title>
  );
};
export default Weather;
